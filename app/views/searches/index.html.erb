<%= @book.errors.any? ? (render 'layouts/errorMessage', model: @book ) : "" %>
<div class='col-4'>
<%= render 'layouts/info', user: current_user, book: @book %>
</div>
<div class="col-8">
	<%= render 'searches/form', search_word: @search_word %>
	<h2>Book containg <%= @search_word %>...</h2>
	<%= @books.exists? ? (render 'books/index', books: @books) : (render 'searches/nohit') %>
</div>